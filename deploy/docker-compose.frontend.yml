services:
  smartcard-front:
    build:
      context: ..
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL}
    container_name: smartcard-front
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:80"
